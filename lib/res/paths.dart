class Paths {
  static const locale = 'assets/locale';

  static const env = '.env';
  static const envDev = '.env.dev';
  static const envLocal = '.env.local';
  static const envProduction = '.env.production';
}
